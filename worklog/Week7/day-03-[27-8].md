# 🗓️ Worklog - Ngày 27/08/2025

## 📅 Thông tin cơ bản

* **Ngày**: 27/08/2025
* **Thứ**: Thứ Tư
* **Tuần thực tập**: Tuần thứ 9/9
* **Thời gian làm việc**: 8:00 – 17:00
* **Mood**: ⚡ Hoàn thiện flow chọn role & tích hợp Swagger OAuth

---

## 🎯 Mục tiêu ngày hôm nay

* [x] Hoàn thiện UI **ChooseRole** trên frontend.
* [x] Kết nối frontend với API `/assign-role`.
* [x] Tích hợp role chọn xong → quay về Swagger UI.
* [ ] Test end-to-end flow signup/login → chọn role → API authorize thành công.

---

## 💼 Công việc đã thực hiện

### 1. Hoàn thiện frontend ChooseRole ⏱️ 8:00–11:00

* **Mô tả**:

  * Viết component `ChooseRole` cho phép user chọn role (`User` hoặc `Author`).
  * Gọi API `/api/role/assign-role` và add user vào group tương ứng trên Cognito.
  * Thêm state loading, alert thông báo thành công hoặc thất bại.

* **Kết quả**:

  * UI hiển thị đầy đủ dropdown role + button Confirm.
  * Gọi API thành công, user được thêm vào group.

---

### 2. Cập nhật App.js để kiểm tra role ⏱️ 11:00–13:00

* **Mô tả**:

  * Thêm state `roleAssigned` để show `ChooseRole` nếu chưa có role.
  * Khi user chọn role xong → cập nhật state + lưu token nếu cần.

* **Kết quả**:

  * Nếu user chưa có role → show `ChooseRole`.
  * Sau khi chọn role → show dashboard / welcome page.
  * Hoàn tất logic redirect sau khi assign role.

---

### 3. Tích hợp flow Swagger OAuth ⏱️ 14:00–16:30

* **Mô tả**:

  * Cập nhật Swagger UI để redirect về sau khi chọn role.
  * Flow: `Authen → đăng ký/login → chọn role → redirect về Swagger UI`.
  * Phân tích vấn đề: Swagger cần authorize lại để lấy token, không share token từ React.

* **Kết quả**:

  * User signup/login xong → chọn role → redirect về Swagger UI.
  * Swagger UI vẫn yêu cầu authorize để dùng token cho request, đúng theo OAuth2 spec.

---

### 4. Kiểm tra & debug flow end-to-end ⏱️ 16:30–17:00

* **Mô tả**:

  * Signup bằng Gmail → chọn role → redirect về Swagger.
  * Test API `/assign-role` và confirm user được thêm vào group.

* **Kết quả**:

  * Flow hoạt động ổn định, role được assign.
  * Swagger UI vẫn cần authorize token để gửi request → đúng chuẩn.

---

## 📚 Kiến thức học được

### 🔧 Technical Skills

* Tích hợp frontend React với API role assignment.
* Sử dụng state + props để quản lý flow chọn role.
* Tối ưu redirect và trải nghiệm người dùng sau khi chọn role.
* Hiểu cơ chế OAuth2: Swagger UI cần authorize token riêng.

### 💡 Concepts & Theory

* Role-based access + Cognito groups + custom attribute `role`.
* Cách build end-to-end signup → role assignment → API access.

### 🤝 Soft Skills

* Debug flow phức tạp liên quan đến frontend + backend + OAuth.
* Chia nhỏ task để kiểm tra từng bước (signup → choose role → API call → redirect).

---

## 🚧 Khó khăn và giải pháp

* **Vấn đề**: Swagger không share token từ React → cần authorize lại.

* **Giải pháp**: Chấp nhận authorize token trong Swagger, nhưng flow React + ChooseRole vẫn mượt.

* **Vấn đề**: Redirect sau khi chọn role → Swagger UI vẫn giữ `code` query cũ.

* **Giải pháp**: Redirect với state mới, không dùng code cũ.

---

## 💭 Reflection & Insights

* **Điều tốt hôm nay**: Hoàn thiện UI chọn role và flow assign role, redirect về Swagger.
* **Khó khăn**: OAuth2 + Swagger không share token giữa client → cần hiểu rõ spec.
* **Bài học**: End-to-end flow gồm signup/login → role → redirect → API test cần kiểm tra token và state từng bước.

**Key Insight**:

> "Dù frontend đã login và chọn role, Swagger vẫn cần authorize lại vì token không share giữa client. Hiểu rõ OAuth2 flow giúp tránh nhầm lẫn khi test API."

---

## 📋 Kế hoạch tiếp theo

### Priority Tasks

* [ ] Test end-to-end với nhiều user mới (Gmail, email/password).
* [ ] Hoàn thiện Lambda PostConfirmation + test tự động add group.
* [ ] Cân nhắc tự động authorize Swagger UI bằng token từ React (nếu cần).

### Learning Goals

* [ ] Nắm vững quy trình: signup → choose role → assign group → API authorize.
* [ ] Hiểu rõ cách integrate Cognito + React + Swagger.
