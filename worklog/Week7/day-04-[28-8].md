

---

# 🗓️ Worklog - Ngày 27/08/2025

## 📅 Thông tin cơ bản

* **Ngày**: 27/08/2025
* **Thứ**: Thứ Tư
* **Tuần thực tập**: Tuần thứ 9/9
* **Thời gian làm việc**: 8:00 – 17:00
* **Mood**: ⚡ Hoàn thiện flow chọn role & fix API 401

---

## 🎯 Mục tiêu ngày hôm nay

* [x] Hoàn thiện logic chọn role sau khi đăng ký và login.
* [x] Sửa Lambda trên Cognito để insert user đúng định dạng mong muốn.
* [x] Sửa các API dựa theo `sub` và `role` để trả dữ liệu chính xác.
* [x] Debug & fix các API bị lỗi 401, cập nhật React để hiển thị đúng.

---

## 💼 Công việc đã thực hiện

### 1. Sửa phần chọn role ⏱️ 8:00–10:30

* **Mô tả**:

  * Khi user đăng ký và chọn role ban đầu là đúng, nhưng sau login vẫn hiển thị lại phần chọn role → đã debug logic kiểm tra role trong DB và token.
  * Cập nhật React component `ChooseRole` + App.js để chỉ show nếu user chưa có role.

* **Kết quả**:

  * Flow chọn role hoạt động ổn định, sau khi assign role xong → hiển thị dashboard đúng.

---

### 2. Sửa Lambda trên Cognito ⏱️ 10:30–12:00

* **Mô tả**:

  * Cập nhật Lambda `PostConfirmation` để insert user vào DynamoDB/Cognito đúng định dạng mong muốn.
  * Thử nghiệm nhiều trường hợp input, fix lỗi format không khớp.

* **Kết quả**:

  * Lambda chạy ổn định, dữ liệu người dùng được insert đúng cấu trúc.

---

### 3. Sửa API dựa theo `sub` và role ⏱️ 13:00–15:00

* **Mô tả**:

  * Sửa các endpoint để lấy dữ liệu theo `sub` từ token và role từ Cognito groups.
  * Thay đổi controller & service để map `sub` làm identifier chính.

* **Kết quả**:

  * API trả dữ liệu chính xác dựa trên `sub` và role, test thành công trên Swagger.

---

### 4. Fix API 401 & React ⏱️ 15:00–16:30

* **Mô tả**:

  * Debug các API bị lỗi 401 liên quan đến `[Authorize(Roles = "User/Author")]`.
  * Map `NameClaimType = "sub"` trong `JwtBearer` để token Cognito được nhận dạng đúng.
  * Cập nhật React hiển thị token và role đúng sau khi login.

* **Kết quả**:

  * API không còn lỗi 401, Swagger UI + React flow hoạt động trơn tru.

---

### 5. Hỗ trợ chỉnh sửa code và review logic ⏱️ 16:30–17:00

* **Mô tả**:

  * Hoàn thiện code controller `AuthorController` mới, thêm API `GetAuthorById`.
  * Tối ưu flow assign role và lấy dữ liệu author.

* **Kết quả**:

  * Endpoint `/api/author/{id}` trả dữ liệu author đúng định dạng.
  * Flow end-to-end từ signup → chọn role → assign group → test API ổn định.

---

## 📚 Kiến thức học được

### 🔧 Technical Skills

* Tích hợp React + Cognito + Swagger OAuth2 flow.
* Debug API role-based access & JWT claim mapping (`sub`, `cognito:groups`).
* Lambda PostConfirmation xử lý dữ liệu DynamoDB đúng định dạng.

### 💡 Concepts & Theory

* Role-based access + Cognito groups + custom attribute `role`.
* End-to-end flow: signup → choose role → assign group → API access.

### 🤝 Soft Skills

* Debug flow phức tạp frontend + backend + OAuth2.
* Quản lý state + token giữa client và API.

---

## 🚧 Khó khăn và giải pháp

* **Swagger không share token từ React** → cần authorize token lại.

  * **Giải pháp**: chấp nhận authorize token riêng trong Swagger, vẫn giữ mượt flow React.
* **Redirect sau khi chọn role** → Swagger giữ `code` query cũ.

  * **Giải pháp**: redirect với state mới, không dùng code cũ.

---

## 💭 Reflection & Insights

* Hoàn thiện UI chọn role, Lambda insert user, API dựa theo `sub` và role.
* OAuth2 + Swagger UI token flow là tricky → hiểu rõ spec giúp tránh nhầm lẫn.
* Bài học: kiểm tra token, claim và state từng bước khi debug flow end-to-end.

> "Dù frontend đã login và chọn role, Swagger vẫn cần authorize lại vì token không share giữa client. Hiểu rõ OAuth2 flow giúp tránh nhầm lẫn khi test API."





## 📋 Kế hoạch tiếp theo

* Xây dựng **tủ truyện (Bookshelf)** cho người dùng: lưu truyện yêu thích, quản lý truyện đã đọc.
* Cải tiến lại một số API:

  * Tối ưu hiệu năng, giảm số query database.
  * Chuẩn hóa format dữ liệu trả về.
  * Đảm bảo role-based access control cho các API liên quan.
* Test end-to-end flow với tủ truyện mới và các API cải tiến.
* Xem xét cải thiện trải nghiệm người dùng trên frontend khi thao tác với tủ truyện.



