

# 🗓️ Worklog - Ngày 25/08/2025

## 📅 Thông tin cơ bản

* **Ngày**: 25/08/2025
* **Thứ**: Thứ Hai
* **Tuần thực tập**: Tuần thứ 9/9
* **Thời gian làm việc**: 8:00 – 17:00
* **Mood**: 🔄 Cấu hình lại AWS và ôn tập kiến thức

---

## 🎯 Mục tiêu ngày hôm nay

* [x] Hoàn thiện API **GetUser**.
* [x] Cấu hình lại toàn bộ AWS (Cognito, DynamoDB, RDS, DNS…).
* [ ] Kiểm tra toàn bộ flow authentication tự động (JWT + Swagger + Cognito).
* [ ] Ghi nhận và ôn tập các kiến thức AWS liên quan.

---

## 💼 Công việc đã thực hiện

### 1. Hoàn thiện API GetUser ⏱️ 8:00–11:00

* **Mô tả**:

  * Triển khai endpoint lấy thông tin user theo `userId`.
  * Sử dụng `IUserService` và `AppDbContext` để truy xuất dữ liệu PostgreSQL.
  * Thêm logging để debug các request.

* **Kết quả**:

  * API GetUser chạy ổn, trả dữ liệu đúng định dạng.
  * Xử lý trường hợp user không tồn tại → trả về `404 Not Found`.
  * Kiểm tra token JWT → user auth thành công.

---

### 2. Cấu hình lại AWS (Cognito, DynamoDB, DNS…) ⏱️ 11:00–15:00

* **Mô tả**:

  * Kiểm tra và đồng bộ thông tin Cognito: `UserPoolId`, `AppClientId`, domain.
  * Cấu hình Swagger UI OAuth2 flow đồng bộ với Cognito.
  * Kiểm tra DynamoDB và kết nối RDS PostgreSQL.
  * Đảm bảo DNS/endpoint AWS được cập nhật đúng vùng `ap-southeast-2`.

* **Kết quả**:

  * Các trigger Lambda (PreSignUp, PostConfirmation) hoạt động bình thường.
  * Token JWT được validate thành công thông qua middleware.
  * Swagger UI có thể authenticate tự động nếu chưa login.

---

### 3. Ôn tập và ghi nhận kiến thức ⏱️ 15:00–17:00

* **Mô tả**:

  * Ghi chú lại các điểm quan trọng khi config Cognito + JWT + Swagger.
  * Ôn tập cách Lambda trigger kết nối PostgreSQL và DynamoDB.
  * Đặt các checkpoint để debug flow authentication trong tương lai.

* **Kết quả**:

  * Hiểu rõ hơn về sequence: `Cognito sign-up → Lambda PostConfirmation → PostgreSQL insert → JWT token flow`.
  * Nhận ra các vấn đề tiềm ẩn liên quan đến `redirect URL`, scopes, PKCE trong Swagger.

---

## 📚 Kiến thức học được

### 🔧 Technical Skills

* Cấu hình Cognito + Swagger OAuth2 + JWT token trong ASP.NET Core.
* Quản lý AWS Lambda trigger (PreSignUp, PostConfirmation) và connect RDS.
* Debug flow authentication tự động qua Swagger UI.

### 💡 Concepts & Theory

* Sequence xử lý user khi dùng Cognito: **Sign-up → Lambda → DB → JWT**.
* Scopes, PKCE, redirect URL quan trọng trong OAuth2 Authorization Code Flow.
* DynamoDB & RDS có thể kết hợp với Lambda cho workflow tự động.

### 🤝 Soft Skills

* Quản lý công việc phức tạp và phân tích từng bước.
* Kỹ năng ghi chú, ôn tập kiến thức khi gặp vấn đề thực tế.

---

## 🚧 Khó khăn và giải pháp

* **Vấn đề**: Đồng bộ Cognito + Swagger OAuth2 có nhiều điểm cần lưu ý (scopes, redirect URL, PKCE).
* **Giải pháp**:

  * Cập nhật các thông số từ AWS chính xác.
  * Bật logging chi tiết trong JwtBearer và Lambda.
  * Test flow từng bước → xác nhận hoạt động bình thường.

---

## 💭 Reflection & Insights

* **Điều tốt hôm nay**: Cấu hình lại thành công toàn bộ AWS và API GetUser chạy ổn.
* **Khó khăn**: Cần ghi nhớ sequence Cognito → Lambda → DB → JWT.
* **Bài học**: Các vấn đề phức tạp đôi khi là cơ hội để ôn tập và hiểu sâu kiến thức nền tảng.

**Key Insight**:

> "Khi làm việc với AWS và authentication flow, việc debug từng bước và ghi chú sequence giúp xử lý sự cố nhanh hơn và tránh sai sót trong production."

---

## 📋 Kế hoạch tiếp theo

### Priority Tasks

* [ ] Test toàn bộ flow authentication từ frontend React.
* [ ] Hoàn thiện logging Lambda cho mọi trigger.
* [ ] Bắt đầu triển khai API CRUD cho các entity khác (Author, Story…).

### Learning Goals

* [ ] Hiểu sâu hơn cơ chế token validation trong ASP.NET Core + Cognito.
* [ ] Nắm rõ sequence Lambda triggers, DB và JWT flow.
