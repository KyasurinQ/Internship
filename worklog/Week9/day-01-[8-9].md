

# 🗓️ Worklog - Ngày 05/09/2025

## 📅 Thông tin cơ bản

* **Ngày**: 05/09/2025
* **Thứ**: Thứ Sáu
* **Tuần thực tập**: Tuần thứ 10/10
* **Thời gian làm việc**: 8:00 – 17:00
* **Mood**: ⚡ Fix bug & mở rộng tính năng API

---

## 🎯 Mục tiêu ngày hôm nay

* [x] Refactor **StoryViewController** để lấy Top Stories.
* [x] Thiết kế và implement API **Follow/Unfollow Author**.
* [x] Viết service **GetTopAuthorsAsync** trả về danh sách tác giả nổi bật.
* [x] Fix lỗi `_context` không tồn tại trong `AuthorService`.
* [x] Thêm DTO `AuthorDto` và entity trung gian `AuthorFollow`.
* [ ] Test đầy đủ các API trên Swagger/Postman.

---

## 💼 Công việc đã thực hiện

### 1. Refactor StoryViewController ⏱️ 8:00–10:00

* **Mô tả**:

  * Chuyển API top stories sang `StoryViewController`.
  * Cho phép query theo `period` (week, month) và `limit`.

* **Kết quả**:

  * API chạy được, trả danh sách top stories.

---

### 2. Follow/Unfollow Author ⏱️ 10:00–12:00

* **Mô tả**:

  * Viết service để user có thể follow/unfollow tác giả.
  * Tạo entity SQL `AuthorFollow` để lưu quan hệ user ↔ author.

* **Kết quả**:

  * API follow/unfollow hoạt động cơ bản.
  * Prevent logic: user không thể tự follow chính mình.

---

### 3. Get Top Authors ⏱️ 13:00–15:00

* **Mô tả**:

  * Implement `GetTopAuthorsAsync` trong `AuthorService`.
  * Trả về danh sách tác giả theo số lượng followers.

* **Kết quả**:

  * API trả đúng dữ liệu `AuthorId`, `Username`, `FollowerCount`.

---

### 4. Fix lỗi `_context` trong AuthorService ⏱️ 15:00–17:00

* **Mô tả**:

  * Trước đó code dùng `_context` không tồn tại.
  * Refactor để dùng `_sql` (`AppDbContext`) đúng chuẩn.
  * Thêm `AuthorDto` và migration cho bảng `AuthorFollows`.

* **Kết quả**:

  * Code compile thành công.
  * Service clean hơn, tách biệt DynamoDB (Story/Chapter) và SQL (Follow).

---

## 📚 Kiến thức học được

### 🔧 Technical Skills

* Cách mix **DynamoDB (NoSQL)** và **SQL (EF Core)** trong cùng project.
* Thiết kế bảng trung gian (join table) để quản lý quan hệ follow.
* Dùng LINQ + EF Core để query top authors.

### 💡 Concepts & Theory

* Tư duy separation of concern:

  * DynamoDB cho dữ liệu content (truyện, chapter).
  * SQL cho quan hệ phức tạp (follow, user).
* DTO giúp tách layer API và DB, tránh lộ entity trực tiếp.

### 🤝 Soft Skills

* Phân tích lỗi compile và trace ngược lại nguyên nhân.
* Refactor code để đồng bộ naming (`_sql` thay vì `_context`).

---

## 🚧 Khó khăn và giải pháp

* **Vấn đề**: `_context` không tồn tại gây compile error.
  *Giải pháp*: Dùng đúng `AppDbContext _sql` thay thế.

* **Vấn đề**: Chưa có bảng trung gian cho follow.
  *Giải pháp*: Tạo entity `AuthorFollow` và chuẩn bị migration.

---

## 💭 Reflection & Insights

* **Điều tốt hôm nay**: Code follow/unfollow + top authors chạy ổn, service clean hơn.
* **Khó khăn**: Vừa phải mix DynamoDB + SQL, dễ nhầm lẫn giữa 2 context.
* **Bài học**: Khi design service, cần tách rõ **dữ liệu content** (NoSQL) và **dữ liệu quan hệ** (SQL).

**Key Insight**:

> "Không phải tất cả logic đều phù hợp với NoSQL – những quan hệ nhiều-nhiều như follow nên để SQL quản lý."

---

## 📋 Kế hoạch tiếp theo

### Priority Tasks

* [ ] Test API end-to-end với Swagger/Postman.
* [ ] Viết controller cho Follow/Unfollow và GetTopAuthors.
* [ ] Tích hợp dữ liệu follow vào dashboard author.

### Learning Goals

* [ ] Lên ý tưởng giao diện frontend:

  * Trang **Author Profile** (Follow/Unfollow).
  * Trang **Top Stories / Top Authors**.
  * Trang **Dashboard Author**.

---


