

# 🗓️ Worklog - Ngày 19/08/2025

## 📅 Thông tin cơ bản

* **Ngày**: 19/08/2025
* **Thứ**: Thứ Ba
* **Tuần thực tập**: Tuần thứ 8/8
* **Thời gian làm việc**: 8:00 - 17:00
* **Mood**: 🛠️ Debug & Hoàn thiện kết nối

---

## 🎯 Mục tiêu ngày hôm nay

* [x] Hoàn thiện migration cho PostgreSQL (EF Core)
* [x] Xác nhận database + tables được tạo trong RDS
* [x] Fix lỗi khi chạy `dotnet ef` trong Package Manager Console
* [x] Test thử API cơ bản với User (Postgres) & Author (DynamoDB)

---

## 💼 Công việc đã thực hiện

### 1. Debug & Migration PostgreSQL ⏱️ 8:00–11:00

* **Mô tả**:

  * Kiểm tra lỗi EF Core không nhận project (`MSB1009`)
  * Xác nhận `.csproj` tên đúng: **AWSWebToon.csproj**
  * Tạo migration `InitDb` + `AddFavoritesTable` thành công
  * Chạy `update-database` push schema lên RDS

* **Kết quả**:

  * PostgreSQL RDS có schema đầy đủ: `Users`, `UserProfiles`, `Transactions`, `Favorites`

---

### 2. Xác minh Database trong RDS ⏱️ 11:00–13:00

* **Mô tả**:

  * Dùng `dotnet ef database drop` để xác định connection tới **RDS** chứ không phải local
  * Truy cập RDS bằng **pgAdmin4** → xác nhận DB `awswebtoon` có tồn tại
  * Cross-check lại với migrations → OK

* **Kết quả**:

  * RDS đã hoạt động, không còn nhầm lẫn với local PostgreSQL

---

### 3. Test API User (Postgres) ⏱️ 13:00–15:30

* **Mô tả**:

  * Gọi API `POST /api/User` để tạo user mới → insert thành công vào RDS
  * Gọi API `GET /api/User/{id}` để đọc lại user vừa tạo
  * Confirm mapping đúng giữa User ↔ UserProfile ↔ Transactions ↔ Favorites

* **Kết quả**:

  * API User chạy ổn, dữ liệu ghi trực tiếp xuống RDS

---

### 4. Test API Author (DynamoDB) ⏱️ 15:30–17:00

* **Mô tả**:

  * Gọi API `POST /api/Author/createstory` để tạo story → ghi vào DynamoDB
  * Test `GET /api/Author/mystories/{authorId}` nhưng gặp lỗi thiếu GSI
  * Debug lại: DynamoDB table **Stories** chưa có GSI cho `AuthorId`

* **Kết quả**:

  * Tạo story OK
  * Cần thêm **Global Secondary Index (GSI)** cho `AuthorId` để query danh sách truyện

---

## 📚 Kiến thức học được

### 🔧 Technical Skills

* Cách chạy EF Core migration chính xác với **RDS** (tránh nhầm local)
* Quản lý schema bằng migration nhiều file (`InitDb`, `AddFavoritesTable`)
* Cách test API User với Postgres và Author với DynamoDB

### 💡 Concepts & Theory

* **RDS = dữ liệu quan hệ** (user, profile, transaction, favorite)
* **DynamoDB = dữ liệu phi quan hệ** (stories, chapters, comments)
* Tầm quan trọng của **GSI** trong DynamoDB để query theo field khác Partition Key

### 🤝 Soft Skills

* **Debugging skill**: xác định chính xác nguyên nhân lỗi `project not found`
* **Detail-oriented**: tránh nhầm local DB và RDS

---

## 🚧 Khó khăn và giải pháp

* **Vấn đề**: Không thấy bảng trong pgAdmin → tưởng migrations không chạy
* **Giải pháp**: Xác nhận lại connection string, drop DB test, login RDS bằng pgAdmin
* **Kết quả**: Đã confirm database và bảng nằm trên RDS

---

## 💭 Reflection & Insights

* **Điều tốt hôm nay**: Đã phân biệt rõ local PG và AWS RDS, push migration thành công.
* **Cần cải thiện**: Cần chuẩn hóa script test API (user, author) để debug nhanh hơn.

**Key Insights**:

> "Khi làm việc với multi-database (SQL + NoSQL), cần test rõ ràng từng luồng dữ liệu để tránh nhầm lẫn connection."

---

## 📋 Kế hoạch tiếp theo

### Priority Tasks

* [ ] Thêm **GSI AuthorIdIndex** cho DynamoDB Stories
* [ ] Hoàn thiện API CRUD cho User (Postgres)
* [ ] Hoàn thiện API CRUD cho Story (DynamoDB)

### Learning Goals

* [ ] Hiểu rõ cách tạo & tối ưu GSI trong DynamoDB
* [ ] Làm quen với repository pattern khi kết hợp EF Core & DynamoDB

---


