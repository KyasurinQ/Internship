

---

# 🗓️ Worklog - Ngày 05/09/2025

## 📅 Thông tin cơ bản

* **Ngày**: 05/09/2025
* **Thứ**: Thứ Sáu
* **Tuần thực tập**: Tuần thứ 10/10
* **Thời gian làm việc**: 8:00 – 17:00
* **Mood**: 🔍 Debug mode & clear mindset

---

## 🎯 Mục tiêu ngày hôm nay

* [x] Fix lỗi `IDynamoDBContext` khi thao tác như Entity Framework.
* [x] Hoàn thiện CRUD cơ bản cho **Story API**.
* [x] Xây dựng CRUD cho **Genre API** (thể loại).
* [ ] Test end-to-end toàn bộ API.

---

## 💼 Công việc đã thực hiện

### 1. Debug `IDynamoDBContext` ⏱️ 8:00–10:00

* **Mô tả**: Fix lỗi không dùng được `Stories` và `SaveChangesAsync`.
* **Kết quả**: Hiểu rằng DynamoDB không phải EF Core, dùng `SaveAsync`, `LoadAsync`, `ScanAsync`.

---

### 2. Story API (CRUD) ⏱️ 10:00–13:00

* Viết lại **CreateStory** với `SaveAsync`.
* Thêm **GetStoryById**, **DeleteStory**, **UpdateStory**.
* Xóa code cũ phụ thuộc vào EF Core.

---

### 3. Genre API (CRUD) ⏱️ 13:00–16:00

* **Mục tiêu**: Quản lý thể loại truyện (Genre).
* **API đã có**:

  * `POST /api/Genre` → tạo genre mới.
  * `GET /api/Genre/all` → lấy danh sách genre.
  * `GET /api/Genre/{id}` → lấy 1 genre theo Id.
  * `PUT /api/Genre/{id}` → update genre.
  * `DELETE /api/Genre/{id}` → xóa genre.
* **Kết quả**: Genre hoạt động chuẩn với DynamoDB.

---

### 4. Tổng kết & định hướng tuần tới ⏱️ 16:00–17:00

* Backend đã có: **Story API** + **Genre API**.
* Tuần sau sẽ chuyển trọng tâm sang **frontend** để hiển thị danh sách Story + Genre.

---

## 📚 Kiến thức học được

### 🔧 Technical Skills

* Phân biệt được cách làm việc của **EF Core (SQL)** và **DynamoDB (NoSQL)**.
* Cách viết CRUD API với DynamoDB (`SaveAsync`, `DeleteAsync`, `QueryAsync`).
* Thiết kế API **Story–Genre** có quan hệ 1-Nhưng (Story gắn nhiều Genre).

### 💡 Concepts & Theory

* DynamoDB không có `SaveChanges()` → mỗi hành động là request riêng.
* Hệ thống thể loại cần tách riêng thành bảng `Genre`, sau đó gắn `GenreId` vào `Story`.

### 🤝 Soft Skills

* Quản lý scope công việc trong ngày: Story xong thì tách thêm Genre.
* Biết cách phân tầng feature để tuần sau chỉ tập trung UI.

---

## 🚧 Khó khăn và giải pháp

* **Vấn đề**: Ban đầu nhầm DynamoDB với EF Core.

* **Giải pháp**: Viết lại API theo DynamoDB context.

* **Vấn đề**: Mapping giữa Story và Genre chưa tối ưu.

* **Giải pháp**: Tạm lưu `GenreIds` trong Story, tuần sau viết UI filter + hiển thị.

---

## 💭 Reflection & Insights

* **Điều tốt hôm nay**: Hoàn thiện cả Story và Genre API.
* **Khó khăn**: Vẫn chưa test toàn bộ flow end-to-end.
* **Bài học**: Làm backend trước (API + DynamoDB) → frontend test sau để tránh mù mờ data.

**Key Insight**:

> "Với DynamoDB, cần tách rõ bảng, sau đó gắn quan hệ bằng Id thay vì join phức tạp như SQL."

---

## 📋 Kế hoạch tiếp theo

### Priority Tasks

* [ ] Bắt đầu làm **frontend** (React/Vue).
* [ ] Tạo UI hiển thị danh sách **Story** kèm **Genre**.
* [ ] Cho phép tạo Story mới với Genre đã chọn.
* [ ] Test toàn bộ flow Create Story → Save DB → Hiển thị ra UI.

### Learning Goals

* [ ] Fetch API từ frontend (axios/fetch).
* [ ] Quản lý state (Story + Genre).
* [ ] Hiểu cách render quan hệ Story–Genre trên UI.

---


