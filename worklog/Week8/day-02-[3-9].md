

---

# 🗓️ Worklog - Ngày 03/09/2025

## 📅 Thông tin cơ bản

* **Ngày**: 03/09/2025
* **Thứ**: Thứ Tư
* **Tuần thực tập**: Tuần thứ 10/10
* **Thời gian làm việc**: 8:00 – 17:00
* **Mood**: 🔐 Debug ban/unban & tích hợp Cognito

---

## 🎯 Mục tiêu ngày hôm nay

* [x] Hoàn thiện logic **ban/unban user** cho hệ thống admin.
* [x] Kiểm tra đồng bộ giữa **Postgres (IsBanned)** và **Cognito (Disable/Enable User)**.
* [ ] Lưu thêm field `CognitoUsername` để quản lý chính xác user từ Cognito.
* [ ] Test lại toàn bộ API admin (get users, ban/unban).

---

## 💼 Công việc đã thực hiện

### 1. Debug ban/unban user ⏱️ 8:00–12:00

* **Mô tả**:

  * Test API `BanUser` và `UnbanUser` với dữ liệu thật từ Postgres + Cognito.
  * Xác định vấn đề `Unban` không hoạt động mặc dù API trả về `200 OK`.
  * So sánh data trong Postgres (`IsBanned`) và Cognito (`Disabled`).

* **Kết quả**:

  * Phát hiện `Ban` chạy được do Cognito nhận email để disable.
  * Nhưng `Unban` thất bại vì Cognito yêu cầu `Username` thực sự (dạng `google_xxxxx`).
  * Cần lưu thêm trường `CognitoUsername` trong DB để fix triệt để.

---

### 2. Refactor model User ⏱️ 12:00–14:00

* **Mô tả**:

  * Thêm field `CognitoUsername` vào entity `User`.
  * Chuẩn bị migration mới để sync DB.
  * Update `BanUserAsync` và `UnbanUserAsync` dùng `CognitoUsername` thay vì email.

* **Kết quả**:

  * Code refactor xong, compile ok.
  * Migration tạo cột mới trong bảng `Users`.
  * Chưa test end-to-end vì cần lưu giá trị `CognitoUsername` từ token login.

---

### 3. Test API & phân tích kết quả ⏱️ 14:00–17:00

* **Mô tả**:

  * Gọi API ban/unban bằng Postman.
  * Check bảng `Users` trong Postgres và user status trong Cognito.
  * Verify xem disable/enable có đồng bộ với flag `IsBanned`.

* **Kết quả**:

  * `Ban` chạy ổn → DB cập nhật `IsBanned = true`, Cognito `Disabled`.
  * `Unban` API trả 200 nhưng Cognito chưa đổi status do chưa truyền đúng `CognitoUsername`.
  * Xác định cần thêm bước lấy `CognitoUsername` từ `IdToken` khi user login.

---

## 📚 Kiến thức học được

### 🔧 Technical Skills

* Làm việc với **AWS Cognito Admin API** (`AdminDisableUser`, `AdminEnableUser`).
* Debug tình huống **API trả 200 nhưng dữ liệu không thay đổi**.
* Migration DB để bổ sung field phục vụ cho việc quản lý user.

### 💡 Concepts & Theory

* Phân biệt **Email** và **Cognito Username** (đặc biệt khi dùng Google login).
* Hiểu rõ sự khác biệt giữa **ban logic trong DB** và **ban thực tế ở Cognito**.

### 🤝 Soft Skills

* Kỹ năng debug multi-system (DB + Cognito).
* Biết cách phân tích root cause khi kết quả thực tế không khớp với response API.

---

## 🚧 Khó khăn và giải pháp

* **Vấn đề**: API `Unban` trả về thành công nhưng Cognito status vẫn `Disabled`.
* **Nguyên nhân**: Sử dụng `Email` thay vì `CognitoUsername` khi gọi `AdminEnableUser`.
* **Giải pháp**: Thêm field `CognitoUsername` trong DB, lưu lại ngay khi user login/register.

---

## 💭 Reflection & Insights

* **Điều tốt hôm nay**: Đã tìm ra nguyên nhân thực sự khiến `Unban` không hoạt động.
* **Khó khăn**: Cognito `Username` phức tạp hơn dự kiến, không thể dùng email trực tiếp.
* **Bài học**: Với các hệ thống identity, phải phân biệt rõ **Username** và **Email**, tránh nhầm lẫn khi thao tác admin.

**Key Insight**:

> "Khi tích hợp Cognito, luôn lưu `CognitoUsername` vào DB để đảm bảo quản lý user chính xác, đặc biệt trong các thao tác admin như ban/unban."

---

## 📋 Kế hoạch tiếp theo

### Priority Tasks

* [ ] Bổ sung logic lưu `CognitoUsername` từ IdToken vào DB khi user login.
* [ ] Test lại API ban/unban end-to-end với `CognitoUsername`.
* [ ] Viết unit test cho `AdminService` (ban/unban user).

### Learning Goals

* [ ] Hiểu cách mapping dữ liệu **Cognito UserPool** ↔ **Postgres DB**.
* [ ] Nắm quy trình quản lý user cross-system (DB + Cognito).

---
