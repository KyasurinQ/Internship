
---

# 🗓️ Worklog - Ngày 22/10/2025

## 📅 Thông tin cơ bản

* **Ngày**: 22/10/2025
* **Thứ**: Thứ Ba
* **Tuần thực tập**: Tuần thứ 17/12
* **Thời gian làm việc**: 8:30 – 17:30
* **Mood**: 🔥 Tập trung debug API và tích hợp frontend React cho phân loại truyện, cảm giác “phê” khi data cuối cùng hiển thị đúng.

---

## 🎯 Mục tiêu ngày hôm nay

* [x] Debug API **GetStoriesByGenre** để hiển thị đúng Manga, Manhua, Manhwa.
* [x] Tối ưu logic frontend React để render truyện theo từng thể loại riêng, tránh lộn xộn.
* [x] Phân loại truyện Hot, Mới và truyện theo từng thể loại (Manga/Manhua/Manhwa) trong section comics.
* [x] Fix trạng thái loading và fallback khi không có dữ liệu.

---

## 💼 Công việc đã thực hiện

### 1. Sáng (8:30 – 11:30) – Debug API phân loại truyện

* Kiểm tra API `/api/Story/ByGenre?genre=Manhwa` và các thể loại khác.
* Thêm log debug để xem dữ liệu từ Postgres (`Genres`, `StoryGenres`) và DynamoDB (`Story`) trước khi trả về.
* Xác định nguyên nhân Manhwa không hiển thị: genre có tồn tại nhưng storyIds chưa map đúng hoặc case-sensitive mismatch.

✅ Kết quả: API hoạt động ổn định cho Manga, Manhua, và Manhwa đã được chuẩn hóa case, trả đúng danh sách story.

---

### 2. Trưa (11:30 – 13:30) – Tối ưu logic frontend React

* Chia state riêng cho từng thể loại: `mangaStories`, `manhuaStories`, `manhwaStories`.
* Sử dụng `Promise.all` để gọi API theo thể loại đồng thời, tránh lặp code hoặc gọi nhiều lần không cần thiết.
* Lọc story đã xóa (`isDeleted`) và chỉ lấy comic (`storyType === 2`) cho section truyện tranh.

✅ Kết quả: Frontend hiển thị Hot, Mới, Manga, Manhua, Manhwa rõ ràng, mỗi truyện vào đúng section, không bị lộn xộn.

---

### 3. Chiều (13:30 – 16:00) – Phân loại Hot & Mới

* Tạo logic sort Hot theo `likes` và Mới theo `storyId` (hoặc `createdAt`) cho section comics.
* Render fallback nếu không có story (ví dụ: “Hiện chưa có truyện nào để hiển thị”).
* Kiểm tra responsive và style section để hiển thị đồng bộ với layout chung.

✅ Kết quả: Hot & Mới hiển thị đúng số lượng 10 story mỗi section, dữ liệu luôn đồng bộ với backend.

---

### 4. Cuối chiều (16:00 – 17:30) – Refactor & polish

* Refactor code frontend để tránh mutate state gốc, clone array trước khi sort.
* Chuẩn hóa icon cho từng thể loại: Manga 🖌️, Manhua 🎨, Manhwa 🖍️.
* Kiểm tra cross-browser và responsive mobile: sections vẫn hiển thị đẹp, không lỗi overflow.

✅ Kết quả: UI gọn gàng, section comics phân loại rõ ràng theo genre, dữ liệu luôn chính xác và có fallback.

---

## 📚 Kiến thức học được

### 🔧 Technical

* Cách debug API phân loại truyện kết hợp Postgres + DynamoDB.
* Sử dụng `Promise.all` để fetch nhiều API đồng thời trong React.
* Tách state và section riêng cho từng thể loại để frontend render rõ ràng.
* Sort array mà không mutate state gốc (`[...array].sort(...)`).

### 💡 Concept

* Phân loại dữ liệu theo genre giúp UX rõ ràng, người dùng dễ tìm truyện.
* Fallback UI và loading state làm frontend trực quan hơn.

### 🤝 Soft Skills

* Tư duy **debug API & frontend binding**, xác định lỗi từ backend hay frontend.
* Kỹ năng **quản lý state phức tạp** trong React, tránh lộn xộn.

---

## 🚧 Khó khăn & Giải pháp

* **Manhwa không trả dữ liệu** → debug bằng log, fix case-sensitive và mapping genreId.
* **Frontend lộn xộn khi nhiều genre** → tách state riêng cho từng thể loại, gọi API Promise.all.
* **Hot & Mới mutate array gốc** → clone array trước khi sort để tránh lỗi render.

---

## 💭 Reflection

* Hiểu rõ cách **tách API & frontend**, render dữ liệu chính xác theo genre.
* Cách **debug backend + frontend đồng bộ** giúp giảm lỗi hiển thị.
* UX tốt hơn khi mỗi truyện vào đúng section, có fallback và responsive.

**Key Insight**:

> “Khi dữ liệu đa nguồn (Postgres + DynamoDB), debug kỹ backend và tách state frontend rõ ràng là chìa khóa để UI chính xác và không lộn xộn.”

---

## 📋 Kế hoạch tiếp theo

📌 **Ngày mai (23/10/2025)**:

* Tích hợp **truyện chữ** (novel) cùng phân loại Hot, Mới và theo genre.
* Thêm **pagination / infinite scroll** cho các section lớn.
* Bắt đầu làm **filter & search** theo genre, rating, author.
