
---

# 🗓️ Worklog - Ngày 23/10/2025

## 📅 Thông tin cơ bản

* **Ngày**: 23/10/2025
* **Thứ**: Thứ Năm
* **Tuần thực tập**: Tuần thứ 17/12
* **Thời gian làm việc**: 8:30 – 17:30
* **Mood**: 🔥 Debug search API và tích hợp gợi ý search realtime, cảm giác “phê” khi input là thấy kết quả liền.

---

## 🎯 Mục tiêu ngày hôm nay

* [x] Debug và fix **search bar** trên header để gợi ý truyện và tác giả hiển thị đúng.
* [x] Fix lỗi fetch `/api/Search` trả về HTML thay vì JSON.
* [x] Thêm debounce và hỗ trợ **Enter key** để search ngay.
* [x] Đảm bảo click ngoài search box sẽ đóng gợi ý.
* [x] Kiểm tra CORS và backend trả đúng JSON cho frontend React.

---

## 💼 Công việc đã thực hiện

### 1. Sáng (8:30 – 11:30) – Debug fetch Search API

* Kiểm tra request fetch `/api/Search?query=Th...` từ frontend.
* Phát hiện lỗi: React nhận được **HTML** thay vì JSON → `SyntaxError: Unexpected token '<'`.
* Nguyên nhân: Backend chưa trả JSON hoặc URL gọi chưa đúng.
* Test trực tiếp endpoint trên trình duyệt: xác nhận backend `/api/Search` phải trả `{ Stories: [...], Authors: [...] }`.

✅ Kết quả: Hiểu rõ nguyên nhân, xác định backend cần trả đúng JSON và đảm bảo frontend gọi đúng URL.

---

### 2. Trưa (11:30 – 13:30) – Fix backend & CORS

* Bật CORS cho frontend (`http://localhost:3000`) trên ASP.NET.
* Chuẩn hóa controller `SearchController` trả đúng JSON.
* Thêm log backend để debug khi có query trống hoặc lỗi.

✅ Kết quả: Backend trả JSON chuẩn, frontend không còn lỗi parse nữa.

---

### 3. Chiều (13:30 – 16:00) – Tối ưu frontend search

* Thêm debounce 250ms để tránh gọi API quá nhiều khi user gõ.
* Thêm xử lý **Enter key** để search ngay lập tức.
* Tách state riêng cho `searchResults` và `searching` để render loading gọn.
* Click ngoài search box sẽ đóng suggestion.

✅ Kết quả: Gõ chữ → thấy gợi ý realtime, nhấn Enter → search, click ngoài → suggestion ẩn.

---

### 4. Cuối chiều (16:00 – 17:30) – Refactor & kiểm tra UI

* Refactor `Header.js` để giữ code gọn, tránh mutate state.
* Kiểm tra responsive: suggestion dropdown hiển thị đúng, không overflow.
* Thêm fallback nếu không có kết quả: “Không tìm thấy truyện hoặc tác giả”.

✅ Kết quả: Search bar mượt, gợi ý hiện ngay khi nhập, tương tác Enter/click ngoài/nhấn nút search đầy đủ.

---

## 📚 Kiến thức học được

### 🔧 Technical

* Cách debug fetch trả HTML thay vì JSON trong React.
* Cấu hình **CORS** backend ASP.NET để cho phép frontend truy cập.
* Debounce trong React + async fetch.
* Xử lý event: Enter key, click ngoài, loading state.

### 💡 Concept

* UX tốt hơn khi suggestion hiện realtime, có loading và fallback.
* Phân tách logic frontend: search query, results, loading state, và dropdown riêng.

### 🤝 Soft Skills

* Debug đồng thời frontend + backend để tìm đúng nguồn lỗi.
* Kỹ năng refactor code React gọn gàng, maintainable.

---

## 🚧 Khó khăn & Giải pháp

* **Fetch trả HTML** → xác định backend URL chưa đúng hoặc redirect SPA → fix endpoint trả JSON.
* **Suggestion không hiển thị** → thêm debounce và đảm bảo state `searchResults` render ngay khi data về.
* **Click ngoài không đóng dropdown** → dùng `useRef` + `mousedown` event listener.

---

## 💭 Reflection

* Hiểu rõ **flow search realtime**: input → debounce → fetch API → render suggestion.
* Khi kết hợp async fetch + React state, cần quản lý debounce, loading và click ngoài để UX mượt mà.
* Việc debug backend + frontend cùng lúc giúp tìm ra lỗi nhanh hơn và fix triệt để.

**Key Insight**:

> “Để search realtime mượt và đúng dữ liệu, backend phải trả JSON chuẩn, frontend debounce + quản lý state gợi ý + xử lý sự kiện đầy đủ.”

---

## 📋 Kế hoạch tiếp theo

📌 **Ngày mai (24/10/2025)**:

* Tích hợp **search kết hợp filter** theo genre, tác giả, rating.
* Bắt đầu triển khai **pagination / infinite scroll** cho kết quả search.
* Kiểm tra UI trên mobile & desktop, đảm bảo suggestion dropdown luôn hiển thị đúng.
