
---

# 🗓️ Worklog - Ngày 15/10/2025

## 📅 Thông tin cơ bản

* **Ngày**: 15/10/2025
* **Thứ**: Thứ Tư
* **Tuần thực tập**: Tuần thứ 15/12
* **Thời gian làm việc**: 8:30 – 17:30
* **Mood**: 💻 Tập trung cao — triển khai logic role & authentication, cập nhật UI phản hồi người dùng, debug lỗi 403 backend.

---

## 🎯 Mục tiêu ngày hôm nay

* [x] Triển khai logic **role assignment** cho user sau khi đăng ký, tránh phải logout/login lại.
* [x] Cập nhật token sau khi assign role để mọi request backend nhận role mới.
* [x] Fix lỗi **403 Forbidden** khi gọi API ngay sau khi assign role.
* [x] Viết lại `App.js` theo hướng **production-ready**, sẵn sàng deploy lên AWS.
* [x] Kiểm tra flow **signinSilent() + fetch backend role** hoạt động mượt.

---

## 💼 Công việc đã thực hiện

### 1. Sáng (8:30 – 11:00) – Phân tích lỗi & design solution

* Quan sát lỗi 403 khi user vừa assign role → gọi API backend.
* Xác định nguyên nhân: **token chưa cập nhật claim role mới**, backend reject request.
* Lên kế hoạch: sau khi assign role → **gọi `signinSilent()`** để refresh token → fetch backend → cập nhật UI.

✅ **Kết quả:** Lên được flow logic tránh logout/login, hướng tới production-ready cho AWS.

---

### 2. Trưa (11:00 – 13:00) – Viết lại App.js

* Refactor `App.js`:

  * Thêm `fetchUserRole()` để lấy role từ backend.
  * Thêm `handleRoleAssigned()` gọi `/api/role/assign-role`, sau đó **refresh token** và fetch role.
  * Giữ logic kiểm tra role từ **token → Cognito groups → backend**.
  * Cập nhật state `roleAssigned`, `userRole` ngay khi token cập nhật.

✅ **Kết quả:** Code gọn, dễ maintain, role được nhận ngay sau assign, user không cần logout/login.

---

### 3. Chiều (13:30 – 16:00) – Test & debug

* Test flow đăng ký user mới:

  1. User đăng ký → redirect chọn role → assign role.
  2. Gọi API backend ngay sau assign → không còn lỗi 403.
  3. UI tự cập nhật, dashboard + author page load dữ liệu chính xác.

* Kiểm tra các page: **AuthorDashboard**, **UserDashboard**, **UserProfile**, **FullStoryForm** → request API nhận token mới, role mới, dữ liệu load mượt.

* Fix lỗi nhỏ: signout redirect URL, fallback khi token claim chưa có role.

✅ **Kết quả:** Flow role assignment + API hoàn thiện, app chạy trơn tru, sẵn sàng deploy AWS.

---

### 4. Cuối chiều (16:00 – 17:30) – Cleanup & documentation

* Cleanup code cũ, xóa log debug, chuẩn hóa export/import.
* Viết comment giải thích **flow role → token → backend fetch**.
* Tối ưu experience: loading state, error state khi backend call fail.

✅ **Kết quả:** App.js production-ready, logic role & auth rõ ràng, API call an toàn, người dùng nhận role ngay sau assign.

---

## 📚 Kiến thức học được

### 🔧 Technical

* Refresh token với **`signinSilent()`** sau khi role thay đổi.
* Quản lý state role & loading state đồng bộ giữa **token & backend**.
* Flow authentication production-ready: token → role → backend → UI.
* Debug Axios + Cognito + React để fix lỗi 403 forbidden.

### 💡 Concept

* User không cần logout/login → trải nghiệm mượt.
* Luồng role assignment + token refresh = chuẩn hóa cho môi trường internet/AWS.

### 🤝 Soft Skills

* Tư duy **debug nhanh**: phân tích nguyên nhân lỗi 403 → tìm giải pháp token refresh.
* Lên kế hoạch viết code **clean & maintainable**, tránh hardcode URL.

---

## 🚧 Khó khăn & Giải pháp

| Khó khăn                                       | Giải pháp                                                                |
| ---------------------------------------------- | ------------------------------------------------------------------------ |
| ⚠️ Token chưa cập nhật claim role → API 403    | ➜ Gọi `auth.signinSilent()` ngay sau khi assign role để refresh token.   |
| 🐛 UI chưa cập nhật role mới ngay              | ➜ Sau silent signin, fetch backend role & cập nhật state `roleAssigned`. |
| 🧩 Routes redirect không chuẩn khi chưa assign | ➜ Thêm `/role` route, redirect logic `Navigate` nếu role chưa assign.    |

---

## 💭 Reflection

* Hôm nay tập trung tối ưu **authentication & role management**: tránh lỗi 403, không cần logout/login, cập nhật UI ngay.
* Hiểu sâu cách **token, claims, backend & React state** phối hợp.
* Sẵn sàng deploy AWS với flow role assignment production-ready.

**Key Insight**

> “Cập nhật role ngay sau assign là quan trọng để user experience mượt, tránh lỗi backend 403 và chuẩn hóa cho deployment.”

---

## 📋 Kế hoạch tiếp theo

📌 **Ngày mai (16/10/2025)**:

* Tích hợp **UI notification** khi có chapter/mới story update (animation đẹp).
* Hoàn thiện **UserProfile / AuthorDashboard** fetch data role mới từ backend.
* Tối ưu UX: loading skeleton, error fallback, highlight khi có role mới.
* Kiểm tra flow deploy trên AWS + HTTPS.
