

# 🗓️ Worklog - Ngày 16/10/2025

## 📅 Thông tin cơ bản

* **Ngày**: 16/10/2025
* **Thứ**: Thứ Tư
* **Tuần thực tập**: Tuần thứ 16/12
* **Thời gian làm việc**: 8:30 – 17:30
* **Mood**: 🛠️ Tập trung cao — xử lý lỗi Cognito PreSignUp & cải thiện hiển thị thông báo lỗi trên giao diện React.

---

## 🎯 Mục tiêu ngày hôm nay

* [x] Tạo component **ErrorNotification** để hiển thị giao diện đẹp cho lỗi PreSignUp từ Cognito.
* [x] Xử lý redirect từ Cognito Hosted UI về React (`/auth/callback`) với trạng thái lỗi.
* [x] Hiển thị message lỗi **"Email đã được sử dụng"** ngay trên giao diện, thay vì chỉ hiển thị query string trên URL.
* [x] Tích hợp ErrorNotification với **RegisterPage** và route **AuthCallback**.
* [x] Cấu hình chính xác **Callback URL** & **Sign-out URL** trên AWS Cognito App Client để tránh lỗi “Something went wrong”.

---

## 💼 Công việc đã thực hiện

### 1. Sáng (8:30 – 11:00) – Cấu hình Callback URL & Sign-out URL

* Kiểm tra danh sách **Allowed callback URLs** trên Cognito:

  * `http://localhost:3000/auth/callback` ✅
  * Xóa các URL dư thừa như `http://localhost:3000` để tránh redirect nhầm.
* Cấu hình **Sign-out URL** trỏ về `/` hoặc `/auth/callback` để xử lý logout.
* Test login/signup từ Hosted UI → xác nhận lỗi PreSignUp redirect về đúng route `/auth/callback`.

✅ **Kết quả:** Callback URL chuẩn hóa, không còn lỗi redirect “Something went wrong”.

---

### 2. Trưa (11:00 – 13:00) – Tạo AuthCallback & ErrorNotification

* Tạo **AuthCallback.jsx** để:

  * Lấy `error_description` từ query string (`URLSearchParams`).
  * Nếu có lỗi, render component **ErrorNotification**.
  * Nếu không lỗi, redirect về `/dashboard`.
* Tạo **ErrorNotification.jsx** với style đẹp:

  * Background đỏ nhẹ, viền đỏ đậm.
  * Title: ❌ Lỗi đăng ký / đăng nhập
  * Hiển thị message từ Cognito (decodeURIComponent).
  * Button quay lại `/` để đăng nhập lại.

✅ **Kết quả:** Giao diện lỗi đẹp, người dùng biết lỗi gì và có thể quay lại trang đăng nhập.

---

### 3. Chiều (13:30 – 16:00) – Tích hợp với RegisterPage & Test

* Trong **RegisterPage.jsx**, khi signUp bị lỗi từ Cognito:

  * Bắt lỗi `err.message.includes("Email này đã được sử dụng")` → setError.
  * Nếu error state có giá trị, render **ErrorNotification** ngay lập tức.
* Test:

  * Đăng ký với email đã tồn tại → ErrorNotification hiển thị đúng.
  * Đăng ký email mới → redirect bình thường, không show lỗi.

✅ **Kết quả:** Lỗi PreSignUp hiển thị đúng giao diện React, UX trực quan.

---

### 4. Cuối chiều (16:00 – 17:30) – Debug & Cleanup

* Kiểm tra:

  * Query string `error_description` decode đúng ký tự tiếng Việt.
  * Button quay lại trang login hoạt động (`navigate("/")`).
* Cleanup code:

  * Xóa console.log thừa, tối ưu CSS inline cho ErrorNotification.
* Đảm bảo route `/auth/callback` hoạt động, không còn crash, lỗi hiển thị mượt.

✅ **Kết quả:** Giao diện lỗi PreSignUp hoàn chỉnh, test toàn bộ flow đăng ký, redirect, hiển thị lỗi, button back → Home hoạt động.

---

## 📚 Kiến thức học được

### 🔧 Technical

* React Router & **useLocation** để đọc query params.
* State & conditional render để hiển thị **ErrorNotification**.
* decodeURIComponent xử lý message lỗi Cognito bằng tiếng Việt.
* Cấu hình **Callback URL** và **Sign-out URL** trên AWS Cognito App Client chuẩn.

### 💡 Concept

* UX tốt = hiển thị lỗi trực quan + người dùng biết cách xử lý → không chỉ báo lỗi trên URL.
* Hosted UI + React cần 1 route trung gian (`/auth/callback`) để render lỗi & redirect.

### 🤝 Soft Skills

* Debug lỗi AWS Cognito Hosted UI → học cách kiểm tra query string & redirect.
* Test toàn bộ flow đăng ký + lỗi + redirect → đảm bảo người dùng không gặp crash.

---

## 🚧 Khó khăn & Giải pháp

| Khó khăn                                                 | Giải pháp                                                         |
| -------------------------------------------------------- | ----------------------------------------------------------------- |
| ⚠️ Hosted UI Cognito redirect lỗi “Something went wrong” | ➜ Chỉ để callback URL chính xác `/auth/callback` và xóa URL dư.   |
| 🐛 Lỗi PreSignUp không hiển thị trên giao diện           | ➜ Tạo component **ErrorNotification** và đọc `error_description`. |
| 🔧 Message tiếng Việt bị encode                          | ➜ Dùng `decodeURIComponent` để render đúng.                       |

---

## 💭 Reflection

* Hôm nay tập trung **tối ưu UX cho lỗi đăng ký** khi email đã tồn tại.
* Hosted UI Cognito cần route trung gian (`/auth/callback`) để React render lỗi và redirect.
* Hiển thị lỗi trên giao diện trực quan giúp người dùng dễ hiểu và thao tác nhanh.

**Key Insight**

> “Một lỗi backend nếu hiển thị trực tiếp trên giao diện đẹp và có hành động rõ ràng sẽ giúp người dùng không bị bối rối và giảm tỷ lệ drop-off.”

---

## 📋 Kế hoạch tiếp theo

📌 **Ngày mai (17/10/2025)**:

* Tích hợp **login với Hosted UI** + redirect về dashboard.
* Thử nghiệm flow **forgot password / reset password** với Cognito.
* Cải thiện **UI RegisterPage**: form validation, loader khi signup.
* Kiểm tra cross-browser & mobile view cho ErrorNotification.
